# Codex Adapter

Installs selected source skills into:

```text
%USERPROFILE%\.codex\skills
```

This adapter never installs or syncs `.system` skills.

## Commands

```powershell
.\install.ps1
.\validate-runtime.ps1
.\sync-from-runtime.ps1
```

Use `-Profile coding` or another profile name to install a subset.
