# Change Contract Template

Use this checklist before nontrivial MATLAB package/tool changes.

## Contract

- Objective:
- Work mode: objective-level / function-level / class-level / refactor
- Accepted design artifact:
- Accepted table row(s) or workflow step(s):
- Affected classes:
- Affected files:
- Public API changes:
- Config changes:
- Backward compatibility expectation:
- Out of scope:

## Implementation Rule

If the change needs a public class, property, method, config field, or behavior not listed above, update the design contract first.
